asgiref==3.9.2
Django==4.2.25
django-cors-headers==4.9.0
django-environ==0.12.0
djangorestframework==3.16.1
sqlparse==0.5.3
typing_extensions==4.15.0
tzdata==2025.2

# ML dependencies for Whisper (STT) and NLLB (translation)
openai-whisper
transformers
sentencepiece
safetensors
accelerate
# CPU torch build; if this fails, install an appropriate wheel for your platform
torch

# Media processing fallback (video/audio extraction)
imageio-ffmpeg
# Optional: python wrapper for ffmpeg, tasks.py can work without it
ffmpeg-python
